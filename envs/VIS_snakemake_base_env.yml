name: VIS_snakemake_base
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.9  # Or the version you’re using
  - snakemake    # For running workflows
  - biopython    # Provides Bio and Bio.Align.Applications
  - pandas       # For data manipulation
  - numpy        # For numerical operations
  - matplotlib   # For plotting
  - seaborn      # For advanced plotting
  - pybedtools   # For working with BED files
  - jsonschema   # For JSON parsing/validation (used by json module)
